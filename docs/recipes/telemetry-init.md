The `telemetry-init.bb` recipe provides an **init script** that starts the telemetry application provided by the `telemetry.bb` recipe.

The application is started by a script we install into `/etc/init.d/`.